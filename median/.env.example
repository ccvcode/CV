# Adresa publică a site-ului (pentru sitemap, RSS, Open Graph)
NEXT_PUBLIC_SITE_URL=https://median.ro
# Opțional: protejează /api/refresh (folosit de cron-ul extern)
CRON_SECRET=
# Opțional: unde se salvează cache-ul de știri (implicit ./.cache)
MEDIAN_DATA_DIR=
