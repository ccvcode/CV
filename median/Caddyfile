{$MEDIAN_DOMAIN:localhost} {
	encode zstd gzip

	# Imaginile procesate se servesc direct de pe disc, cu cache lung.
	handle_path /media/* {
		root * /data/median/media
		header Cache-Control "public, max-age=31536000, immutable"
		file_server
	}

	reverse_proxy web:3000
}
